/*
    Crie um programa C, para ler duas variaveis (A, B),
    referencie a idade de duas pessoas. Crie um ponteiro para *ps.
    Armazene a soma dos valores (A, B) em um espaço de memória aloca_
    cado dinamicamente (use malloc) e referenciada pelo ponteiro *ps, após:

    1. Mostre o resultado de A+B;
    2. Mostre oendereçoarmazenado no ponteiro *ps;
    3. Mostre o endereço do ponteiro *ps;
    4. Mostre o endereço das variaveis A e B.


*/

#include <stdio.h>
#include <stdlib.h>


int main () {
    
}