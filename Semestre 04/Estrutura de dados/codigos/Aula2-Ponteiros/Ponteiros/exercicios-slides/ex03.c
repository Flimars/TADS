/*
    Crie um programa em C, para ler duas variaveis double (A, B), 
    referente ao peso de duas pessoas;
    Aloque uma opção de memória do tipo double, atribua o endereço  
    da menoria alocada ao ponteiro *ps, para armazenar a soma dos valores A e B.

    1. Mostre o  resultado de A + B;
    2. Mostre o  endereço  armazenado no ponteiro *ps, e  o valor referenciado;
    3. Crie uma variavel double C, e copie o valor referenciado no ponteiro *ps
    para a variavel C. Mostreo valor de C.

*/


#include <stdio.h>
#include <stdlib.h>


int main () {
    
}